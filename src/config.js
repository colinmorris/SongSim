import {MODE} from './constants.js';

var config = {
  alleys: true, 
  colormap: 'warm',
  default_mode: MODE.colorful,
  debug: true,
  exportSVGEnabled: false,
  checkerboard: true,
};

export default config;
